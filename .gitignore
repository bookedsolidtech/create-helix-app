node_modules
node_modules/
dist
dist/
.cache/
*.tgz
.DS_Store
.env
.env.*
!.env.example
coverage/
*.log
.turbo/
# Worktree directories (managed by protoLabs Studio)
.automaker/
.claude/worktrees/
.worktrees/

# protoLabs Studio local files
.automaker-lock
protolab.config
proto.config.yaml
protoLabs.report.html
spec.md

# Internal docs
PE-REVIEW.md

# Temp files
test-scaffold.ts
.pnpm-store
.act-results.json

# Session context (not committed)
@RESTART.md

# === rea managed — do not edit between markers ===
.rea/audit.jsonl
.rea/audit-*.jsonl
.rea/HALT
.rea/metrics.jsonl
.rea/serve.pid
.rea/serve.state.json
.rea/fingerprints.json
.rea/review-cache.jsonl
.rea/*.tmp
.rea/*.tmp.*
.rea/install-manifest.json.bak
.rea/install-manifest.json.tmp
.gitignore.rea-tmp-*
.rea.lock
.rea/last-review.json
# === end rea managed ===

# Runtime caches not yet covered by rea-managed block
.rea/last-review.cache.json

# Claude Code scheduled-task runtime state
.claude/scheduled_tasks.lock

# Local-only Figma screenshots from MCP probes (figgy debugging)
tmp-screenshots/

# Local-only cross-kit audit fixtures (v0.9.1 audit sweep)
tmp-audits/

# SBOM artifact — generated by the release workflow (and pnpm run sbom locally).
# Uploaded as a release artifact in release.yml; not source.
sbom.json

# Benchmark output — generated by `vitest bench --outputJson` (pnpm bench:ci).
# Uploaded as a CI artifact in ci.yml; not source.
bench-results.json

# Stray npm lockfile — this repo standardizes on pnpm (pnpm-lock.yaml). The
# cyclonedx-npm SBOM step that once required it was removed in v0.9.4.
package-lock.json
