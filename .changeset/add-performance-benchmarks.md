---
'create-helix': patch
---

Add performance benchmarks using vitest bench mode for scaffold time (all 15 frameworks), template resolution, project name validation, and config file parsing; includes baseline tracking and CI regression detection (warn-only, >20% threshold)
