---
'create-helix': minor
---

Add programmatic API export (`create-helix/api`) for CI/CD pipelines and build tools. Exports `scaffold()`, `listTemplates()`, `listPresets()`, `getTemplate()`, and `validate()` — all pure functions with no `process.exit` calls and no TUI output.
