---
'create-helix': minor
---

Add graceful degradation and offline mode support: detect offline state at startup, skip network checks in doctor, use cached registry data in upgrade, and add --offline flag.
