---
'create-helix': patch
---

Add comprehensive unit tests for CLI command modules: `info`, `list`, `config-validate`, and `upgrade`. Tests cover command output (TUI and JSON modes), error cases (missing files, network failures), validation logic, and upgrade orchestration.
