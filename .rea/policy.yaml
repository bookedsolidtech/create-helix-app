# .rea/policy.yaml — managed by rea v0.6.1
# Edit carefully: tightening takes effect on next load; loosening requires human approval.
version: "1"
profile: "bst-internal"
installed_by: "himerus"
installed_at: "2026-04-20T15:54:27.553Z"
autonomy_level: L3
max_autonomy_level: L3
promotion_requires_human_approval: true
block_ai_attribution: true
blocked_paths:
  - ".env"
  - ".env.*"
  - ".rea/policy.yaml"
  - ".rea/HALT"
  - "CODEOWNERS"
  - ".github/workflows/release.yml"
  - "SECURITY.md"
  - "THREAT_MODEL.md"
notification_channel: ""
injection:
  suspicious_blocks_writes: true
context_protection:
  delegate_to_subagent:
    - "pnpm run build"
    - "pnpm run test"
    - "pnpm run lint"
  max_bash_output_lines: 100
review:
  concerns_blocks: true
  codex_required: true
