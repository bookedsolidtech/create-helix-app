# ADVISORY: rea 0.25.0 ships architect agent tier

**Status:** advisory (not a bug — positive finding)
**Reported:** 2026-05-05T03:28:00Z
**Reporter:** create-helix-app monitoring loop (cron `c1da923e`, first run)
**rea version:** 0.25.0 (installed: 0.24.0)
**Severity:** low (informational)

## Summary

rea 0.25.0 ships three new "architect tier" curated agents: `data-architect`, `devex-architect`, `platform-architect`. The rea-orchestrator was also updated. Doctor reports clean on the new version (the only failures are worktree-context artifacts, not real regressions).

## Reproduction

1. From feature/wc-storybook-land HEAD (rea 0.24.0):
2. `git worktree add .claude/worktrees/rea-monitor-0.25 HEAD`
3. `cd .claude/worktrees/rea-monitor-0.25 && pnpm update @bookedsolid/rea@0.25.0 && npx rea doctor --drift`
4. Drift report shows:

```
[missing] .claude/agents/data-architect.md  — file not installed
[missing] .claude/agents/devex-architect.md  — file not installed
[missing] .claude/agents/platform-architect.md  — file not installed
[drifted-from-canonical] .claude/agents/rea-orchestrator.md  — local matches manifest but differs from rea v0.25.0 canonical
```

43 clean, 4 with drift. Doctor's "hard fail" was on `pre-push hook installed` — but that's because git worktrees lack `.git/hooks/` by default; husky/rea expect `.git/hooks/` or `core.hooksPath=.husky`. **Not a 0.25.0 regression** — same behavior on 0.24.0 in a worktree.

## Expected

3 new agents available to enrich the curated roster.

## Actual

Same — works as expected.

## Root cause hypothesis

n/a — this is a feature-add release, not a defect.

## Suggested fix

Apply the upgrade in main checkout:

```bash
pnpm update @bookedsolid/rea@0.25.0
npx rea upgrade --force
npx rea doctor
git add .claude/agents/ .rea/install-manifest.json package.json pnpm-lock.yaml
git commit -m "chore(rea): upgrade 0.24.0 -> 0.25.0 — architect agent tier"
```

Then verify the new agents are dispatchable via `Agent({ subagent_type: 'platform-architect', ... })` etc. Update CLAUDE.md or memory if the team workflow now incorporates these.

## Workaround

Stay on 0.24.0 until validated. Architect agents were a planned addition — Jake explicitly mentioned wanting them ("Architect level should be next") in 2026-05-04 session.

## Cross-references

- Prior upgrade: 0.11.0 → 0.24.0 in commit `2fad785` (2026-05-04)
- BUG-013.0 (interior `/./` bypass in 0.13.0): resolved in 0.14+, irrelevant on 0.25
- User directive (2026-05-04): "Architect level should be next."

## Discovery context

First run of the 1hr monitoring loop (cron `c1da923e`, armed 2026-05-05 03:25Z, fires every hour at :07 minute). Loop detected version delta 0.24.0 → 0.25.0 and ran the drift test in worktree per its prompt.
