/**
 * v0.8.0 Phase A — Astro scaffolder fork shared module.
 *
 * Placeholder. Phase C populates this with monorepo-specific helpers
 * for the apps/web emit (Astro config + Layout.astro + landing page
 * overlays). The flat scaffolder body stays in src/scaffold.ts and is
 * re-exported via flat.ts for now.
 */
export {};
