/**
 * v0.8.0 Phase A — Astro monorepo scaffolder stub.
 *
 * Phase C fills the body. Until then, calling
 * scaffoldProject({framework: 'astro', monorepoMode: true}) throws so
 * the dispatch test can verify the routing is wired without producing
 * partial scaffolds.
 */
import type { ProjectOptions } from '../../types.js';

export async function scaffoldAstroMonorepo(_options: ProjectOptions): Promise<void> {
  throw new Error('astro monorepo scaffolder not yet implemented — runs in v0.8.0 Phase C.');
}
